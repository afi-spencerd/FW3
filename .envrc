# Auto-load the pinned dev shell (Node, pnpm, Prisma engine env) via direnv.
# Run `direnv allow` once to enable. Requires nix-direnv for caching.
use flake

# Load local secrets / connection strings if present (never commit .env).
dotenv_if_exists .env
